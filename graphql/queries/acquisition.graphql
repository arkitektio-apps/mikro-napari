query get_image_stage($id: ID!, $limit: Int) {
  stage(id: $id) {
    positions {
      x
      y
      z
      omeros(order: "-acquired", limit: $limit) {
        acquisitionDate
        representation {
          id
          shape
        }
      }
    }
  }
}
